export type RatingsData = {
    [key: string]: string;
};

export type Indicator = {
    Category: string,
    CategoryGroup: string;
};

export type TableConfig = {
    rounded?: boolean,
    pagination?: boolean,
    borderless?: boolean,
};
